// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'placehold.co',
      'cdn.pixabay.com',
      'images.pexels.com',
      'images.unsplash.com'
    ],
  },
};

module.exports = nextConfig;
